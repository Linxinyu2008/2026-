# 第一问：交会定位区域

`localization_region.py` 实现有界示向误差、半平面交集、定位区域直径和最小覆盖圆。

运行：

```powershell
python -m questions.q1.localization_region
```

问题2通过 `questions.q1.localization_region.solve_localization_region` 复用这里的几何求解器。
