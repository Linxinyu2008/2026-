# B题代码修复记录

本记录对应同目录的离线审查报告。修复未连接官方模拟器，也没有开展演练或正式测试。

第一问现在先求可行点再判定是否无界，因此不相容的示向角域返回空集。第三问的区域摘要改为直接复用第一问求解器，不再把无界交会区域截成有限多边形。

第二问的射线粒子会同时裁剪进出目标圆的两个交点，支持首测点在区域外；筛选时加入可行距离两端，最坏直径为无穷大的候选会被拒绝。粒子法仍是离散近似，文档中不再把粒子覆盖率称为连续空间的严格保证。

第三问训练环境在无显式种子重置时使用下一个种子，PPO不再反复训练同一场景；误差场改为地点相关、时间固定并受±1°约束。评测使用本地场景真实目标数作统计分母，输出平均定位清除时间，清除动作成本不再错误加入频道切换。官方控制器在已成功进入后发生异常时会尝试收尾退出，且把宏决策步与HTTP命令数分开。

第四问代码不纳入本次仓库；题目与建模文字材料单独保留。

验证结果：第一问1项、第二问10项、第三问23项单元测试通过。统一回测3个混合场景全部完成清除。PPO重新训练10000步，独立20个混合场景评测中全部清除；模型、元数据和回测结果保存在 `B题问_代码/outputs/route_c/20260911_043624` 与 `outputs/route_c/backtest/20260911_043705`。
